<% @links.each do |link| %>
 <div class="row link clearfix">
   <h2>
     <%= link_to link.title, link %><br>
     <small class="author">
      Submitted <%= time_ago_in_words(link.created_at)%> by <%= link.user.email if link.user %>
     </small>
   </h2>
 </div>  
<% end %>

